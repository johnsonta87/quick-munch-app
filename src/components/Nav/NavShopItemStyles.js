import styled from 'styled-components';

export const NavShopItemStyles = styled.li`
  list-style: none;

  > button {
    cursor: pointer;
    padding: 25%;
    background: transparent;
    border: 0;

    svg {
      width: 1.5em;
      height: 1.5em;
    }
  }
`;
